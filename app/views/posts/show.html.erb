<div class="view_post">
	<p>Post #<%= @post.id %> by <%= @user.username %>.</p>
	<div class="picture">
		Image goes here!
	</div>
	<h2>
		Car: <%= @post.year.to_s + " " + @make.name + " " + @car.name %>
	</h2>
	<h2>
		Dealer: <%= @dealer.name %><br/>
		<%= @dealer.address %>, <%= @dealer.city %><br/>
		<%= @dealer.province %>, <%= @dealer.zip %>
	</h2>
	<h2 class="price_paid">
		Price Paid: $<%= @post.price_paid %>
	</h3>
	<h2>
		<%= @user.username %> says...
	</h2>
	<div class="description">
		<p>
			<%= @post.description %>
		</p>		
	</div>
</div>